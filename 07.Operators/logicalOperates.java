public class logicalOperates {
    public static void main(String args[]){
        // Logical Operates:
        // 1. && -> and operator ; true iff both true
        // 2. || (double pipe) -> or operator ; true iff one true
        // 3. ! -> not operator ; !(true) -> false ; !(false) -> true

        // System.out.println((5>0) && (0>1));
        // System.out.println((5>0) || (0>1));
        // System.out.println(!(5>0));
        System.out.println(!(5<0));

     }
}
